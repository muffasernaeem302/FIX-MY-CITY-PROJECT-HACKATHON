# ============================================================
#  FixMyCity — Environment Variables Template
# ============================================================
# Copy this file to .env in the respective directory.
# NEVER commit .env files to version control.
# ============================================================

# --- Backend defaults (reference only — real values in backend/.env) ---
# DATABASE_URL=postgresql+psycopg2://fixmycity:fixmycity@localhost:5432/fixmycity
# AUTH_SECRET=change-me-to-a-random-32-byte-secret-string
# AI_PROVIDER=dev_fallback
# STORAGE_DRIVER=local

# --- Frontend defaults (reference only — real values in frontend/.env) ---
# VITE_API_BASE_URL=http://localhost:8000/api
